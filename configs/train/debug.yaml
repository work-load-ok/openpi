# Debug Config
# A minimal config for testing and debugging

_target_: TrainConfig
name: debug

# Model configuration with dummy variants for fast testing
model:
  _target_: Pi0Config
  paligemma_variant: dummy
  action_expert_variant: dummy

# Fake data for testing
data:
  _target_: FakeDataConfig

# Debug settings
batch_size: 2
save_interval: 100
overwrite: true
exp_name: debug
num_train_steps: 10
wandb_enabled: false

